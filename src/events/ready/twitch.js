// const { Twitch } = require("@voidpkg/social-alert");
const { ButtonBuilder, ButtonStyle, ActionRowBuilder, EmbedBuilder } = require('discord.js')
// const SocialAlerts = require('../../models/social-alert.js')

module.exports = async (client, c) => {
    // const twitch = new Twitch({
    //     interval: 5000,
    //     client:{
    //         id: process.env.TWITCH_ID,
    //         secret: process.env.TWITCH_SECRET,
    //         token: process.env.TWITCH_TOKEN
    //     },
    //     channels: ['']
    // })
   
    // const twitchAlerts = SocialAlerts.find({ social: 'twitch' })
    // if (twitchAlerts) {
    //     twitchAlerts.forEach(async (alert) => {
    //         const guild = await c.guilds.fetch(alert.guildId)

    //         const channel = await guild.channels.fetch(alert.channelId)

    //         if (channel) {
    //             con
    //         }
    //     })
    // }
};
